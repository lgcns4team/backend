# Copy this file to `.env` and fill in real values. DO NOT COMMIT the real `.env` with secrets.

# JDBC driver
DRIVER_NAME=org.mariadb.jdbc.Driver
DRIVER_PASSWORD=

# Docker compose DB credentials
DB_COMPOSE_ROOT_PASSWORD=
DB_COMPOSE_USER=noknok_user

# Optional: other env vars used by scripts/compose
# DB_COMPOSE_PASSWORD= (if used)
# SPRING_PROFILES_ACTIVE=dev
